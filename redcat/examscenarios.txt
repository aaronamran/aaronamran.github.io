📘 SCENARIO 1
You are the system administrator of a small research server called server1.example.com.
A second system, server2.example.com, acts as a remote host for SSH, NFS, and file transfers.
Both systems share the same root password: RedHat123.

Your task is to configure server1 to meet the following requirements.

🧪 SECTION 1 — ESSENTIAL TOOLS
1. Basic Shell Usage
Create a directory /practice/tools.

Inside it, create a text file summary.txt containing exactly 3 lines:

nginx
Copy code
RHCSA Practice
Essential Tools
Completed
2. I/O Redirection
List all files in /etc containing the string “conf” and save the output to
/practice/tools/conf_list.txt.

Append the sorted list of all users in /etc/passwd (first column only) to
/practice/tools/conf_list.txt.

3. grep & Regex
Extract all lines in /var/log/messages matching a date pattern
(YYYY-MM-DD) and save them to /practice/tools/date_matches.log.

4. Archiving
Create an archive of /practice/tools at /root/tools_backup.tar.gz.

🧪 SECTION 2 — SOFTWARE MANAGEMENT
5. RPM Repositories
Configure a new repository named localrepo with the following:

Base URL: /repo

Enabled: yes

GPG check: disabled

(Assume /repo already contains valid RPM content.)

6. RPM Packages
Install tmux and tree.

Remove the sl package if present.

7. Flatpak
Add the Flathub remote using:

arduino
Copy code
https://flathub.org/repo/flathub.flatpakrepo
Install the Flatpak application com.visualstudio.code.

🧪 SECTION 3 — SHELL SCRIPTING
8. Basic Script
Create /usr/local/bin/checkusers.sh with the following behavior:

Takes one argument (a username).

If the user exists → print:
User <name> exists

Else → print:
User <name> not found

Make the script executable.

9. Looping Script
Create /usr/local/bin/count_lines.sh:

Reads each file in /etc ending with .conf.

For each, prints:
<filename>: <linecount>

🧪 SECTION 4 — OPERATING RUNNING SYSTEMS
10. Boot Targets
Configure system to boot into multi-user.target permanently.

11. Identify Resource-Heavy Processes
Identify the highest CPU-consuming process and redirect the output to
/root/highcpu.txt.

12. Journals
Configure journald so logs persist across reboots.

13. Services
Ensure sshd is running and enabled at boot.

Stop the avahi-daemon and disable it at boot.

🧪 SECTION 5 — STORAGE
A new 10 GB virtual disk (/dev/sdb) has been attached.

14. Partitions
Create a GPT partition on /dev/sdb1 of size 2 GB.

15. LVM
Create:

PV: /dev/sdb1

VG: researchvg

LV: data size 1.5G

Format /dev/researchvg/data as xfs.

16. Persistent Mounts
Mount it permanently at /mnt/research.

Update /etc/fstab using its UUID.

17. Extend LV
Extend LV data by 300M and grow the filesystem.

18. Swap
Create a 500M swap LV named swaplv in the same VG.

Enable it and ensure it auto-mounts at boot.

🧪 SECTION 6 — FILESYSTEMS & NETWORK FILESYSTEMS
19. Local Filesystems
Create a FAT filesystem at /dev/sdb2 (size 500M) and mount it at /mnt/fatdrive.

20. NFS
Assume server2 exports:
server2:/srv/nfs/share

Mount it temporarily at /mnt/share.

Configure autofs:

Master map: /etc/auto.master.d/lab.autofs

Mount point: /nfslab

Map: /etc/auto.lab

Key: share

Remote path to server2 export

🧪 SECTION 7 — TASK SCHEDULING
21. Cron
Create a cron job for user root:

Runs at 3:45 AM daily

Executes /usr/local/bin/checkusers.sh root
and appends output to /var/log/usercheck.log

22. systemd Timer
Create a systemd timer named diskreport.timer that:

Runs every 10 minutes

Executes /usr/local/bin/diskreport.sh

The script writes current disk usage to /var/log/diskusage.log

🧪 SECTION 8 — BASIC NETWORKING
23. Network Configurations
Configure:

IPv4: 192.168.55.10/24

Gateway: 192.168.55.1

DNS: 8.8.8.8

24. Hostname Resolution
Set hostname to server1.example.com.

25. Firewalld
Open the http, https, and nfs services.

Remove access to the cockpit service if present.

🧪 SECTION 9 — USERS & GROUPS
26. User Accounts
Create:

User analyst with password Test123!

User developer with password Code123!

Create group research. Add analyst to it.

27. Password Aging
Set password expiry for analyst:

Max days: 30

Warning: 7 days

28. Sudo Access
Configure:

Members of group research can run /usr/bin/systemctl without password

🧪 SECTION 10 — SECURITY & SELinux
29. SSH Keys
Configure key-based authentication:

Generate SSH key for root on server1

Copy key to server2 for passwordless login

30. SELinux Basics
Set SELinux to enforcing mode.

Restore default context of /mnt/research.

31. SELinux Boolean
Enable boolean httpd_can_network_connect permanently.

32. SELinux Ports
Add a custom http port 8081 to SELinux and allow HTTPD to use it.

🧪 COMPLETE TASK
Reboot and confirm all configurations persist.


----------------------------------------------------------------


🟦 RHCSA Practice Exam Simulation #2
Time Allowed: 3 hours
Systems:

serverA.example.com (you work on this system)

serverB.example.com (remote host for SSH & NFS)

Root password for both: Practice123

All configurations must persist after reboot.

🔵 SCENARIO
You have just joined the IT operations team. Your task is to configure serverA to meet internal infrastructure requirements.
The second machine, serverB, is reachable via SSH and provides services used throughout the company.

🟧 SECTION 1 — ESSENTIAL SYSTEM TOOLS
1. Shell & Files
Create directory /lab/system.

In this directory, create a file notes.txt containing:

csharp
Copy code
RHCSA Simulation Exam 2
Essential Tools Section
Completed by <yourname>
2. Redirection
Redirect all lines in /etc/fstab containing the word boot into /lab/system/bootlines.txt.

Append the output of:

bash
Copy code
ls -1 /usr/bin | wc -l
to the same file.

3. grep & regex
Extract all IPv4-like patterns from /var/log/secure and save to /lab/system/ip_matches.log.

4. Archiving
Create a bzip2-compressed tar archive /root/system_archive.tar.bz2 containing all files inside /lab/system.

🟧 SECTION 2 — SOFTWARE MANAGEMENT
5. RPM Repository
Create a repository file /etc/yum.repos.d/lab.repo:

name: labrepo

baseurl: /packages

enabled: 1

gpgcheck: 0

(Assume /packages already contains valid RPMs.)

6. Install & Remove Packages
Install wget and lsof.

Remove the httpd package if installed.

7. Flatpak
Add Flatpak remote:

arduino
Copy code
https://dl.flathub.org/repo/flathub.flatpakrepo
Install the Flatpak app org.gnome.Calculator.

🟧 SECTION 3 — SHELL SCRIPTING
8. Script: Conditional
Create /usr/local/bin/usercheck.sh that:

Accepts a username as argument

If user exists: prints
User <username> exists

Else: prints
User <username> does not exist

Make it executable.

9. Script: Loop & Command Substitution
Create /usr/local/bin/service_report.sh that:

Loops through all services starting with ss

For each service, prints:
<service>: <active/inactive>

🟧 SECTION 4 — OPERATING RUNNING SYSTEMS
10. Boot Targets
Set system to boot into graphical.target by default.

11. Boot Troubleshooting
Configure the system so that the GRUB menu is visible for 10 seconds during boot.

12. Process & Journals
Generate a list of top 5 memory-consuming processes and save to /root/memtops.txt.

Ensure persistent journaling is enabled.

13. Service Management
Enable and start chronyd.

Stop and disable cups.

🟧 SECTION 5 — STORAGE
A new disk /dev/sdc (8 GB) has been added.

14. Partitioning
Create a single 3 GB GPT partition on /dev/sdc: /dev/sdc1.

15. LVM Setup
Create:

PV: /dev/sdc1

VG: opsvg

LV: data2 (size 2.5G)

Format LV with ext4.

16. Persistent Mount
Mount it permanently at /mnt/data2 using its label (label must be data2vol).

17. Extend Filesystem
Extend LV data2 by 500M and grow filesystem accordingly.

18. Swap
Create a 400M swap file /swapfile1.

Enable it and ensure permanent activation.

🟧 SECTION 6 — FILESYSTEMS & NETWORK FILESYSTEMS
19. Local Filesystems
Create an XFS filesystem on /dev/sdc2 (size 1G), mount at /xfsdata.

20. NFS
serverB exports NFS path:
serverB:/export/shared

Mount it temporarily at /mnt/sharedtmp.

21. autofs
Configure autofs:

Master map: /etc/auto.master.d/shared.autofs

Mount point: /shared

Map file: /etc/auto.shared

Key: public

Target: serverB:/export/shared

🟧 SECTION 7 — TASK SCHEDULING
22. Cron
Create a cron job for root:

Runs every 15 minutes

Executes /usr/local/bin/service_report.sh

Output appended to /var/log/service_status.log

23. systemd Timer
Create a systemd timer/unit pair named backupdata:

Timer runs at 02:00 every day

Unit executes /usr/local/bin/backup.sh

Script must archive /etc to /var/backups/etc_<date>.tar.gz

(You must create backup.sh.)

🟧 SECTION 8 — NETWORKING
24. IP Configuration
Configure:

IPv4: 10.20.30.40/24

Gateway: 10.20.30.1

DNS: 1.1.1.1 and 8.8.4.4

25. Hostname Resolution
Configure the hostname to serverA.example.com.

26. firewalld
Add permanent rules to allow:

ssh

nfs

dhcpv6-client

Remove access to samba service if present.

🟧 SECTION 9 — USERS & GROUPS
27. Local Users
Create:

User ops1 with password Ops123!

User ops2 with password Ops234!

Create group operations.
Add ops1 and ops2 to this group.

28. Password Aging
Configure:

ops2: max age 45 days

ops2: warn 5 days before expiry

29. sudo
Members of operations should be allowed to run /usr/bin/yum without a password.

🟧 SECTION 10 — SECURITY & SELinux
30. SSH Key Authentication
Generate SSH keys for user root on serverA.

Copy the public key to root@serverB for passwordless login.

31. SELinux: Contexts
Restore default SELinux context on /mnt/data2.

32. SELinux: Boolean
Enable SELinux boolean ftp_home_dir permanently.

33. SELinux: Ports
Configure SELinux so that service ssh is allowed to run on port 2222 in addition to port 22.

🟦 Final Requirement
46. Reboot the system.
Verify all configurations still work after reboot.


---------------------------------------------------------------------------


🟥 RHCSA Practice Exam Simulation #3
Time Allowed: 3 hours
Systems:

primary.example.com (your machine)

remote.example.com (NFS + SSH target)

Root password for both: LinuxLab123

All tasks must survive reboot.

🟦 SCENARIO
You are the system administrator of primary.example.com, a general-purpose compute node used by internal developers.
You will prepare the system to meet new organizational compliance requirements.

A second machine, remote.example.com, provides:

SSH endpoint

An NFS export

Shared configuration repository

You must configure primary according to the following requirements.

🟧 SECTION 1 — ESSENTIAL TOOLS
1. Shell & File Management
Create directory /work/essentials.

Create a file /work/essentials/intro.txt containing:

css
Copy code
RHCSA Simulation Exam 3
Essentials Section
<yourname>
2. Redirection & Processing
Save all lines from /etc/ssh/sshd_config containing the word “Permit” into /work/essentials/ssh_permit.txt.

Append the number of active system users (from who | wc -l) to the same file.

3. grep & Regex
Extract all usernames with UID < 500 from /etc/passwd using regex and save to /work/essentials/lowuid.txt.

4. Archiving
Create a gzip archive /root/essentials_backup.tar.gz from the /work/essentials directory.

🟧 SECTION 2 — SOFTWARE MANAGEMENT
5. Repositories
Configure an internal repo by creating /etc/yum.repos.d/internal.repo:

name: internalrepo

baseurl: /internalrepo

enabled: 1

gpgcheck: 0

6. Package Installation & Removal
Install packages: vim-enhanced, bash-completion.

Remove package chrony if installed.

7. Flatpak
Add the Flatpak remote located at:

arduino
Copy code
https://flathub.org/repo/flathub.flatpakrepo
Install the Flatpak application org.gnome.Terminal.

🟧 SECTION 3 — SHELL SCRIPTING
8. Script: Conditionals
Create script /usr/local/bin/checkport.sh:

Accepts a port number as argument.

If any process is listening on that port → print:
Port <port> is in use

Else → print:
Port <port> is free

Make the script executable.

9. Script: Loops & Command Substitution
Create /usr/local/bin/dirsize.sh that:

Loops through all directories under /home

For each directory: print
<dirname>: <disk usage in MB>

🟧 SECTION 4 — OPERATING RUNNING SYSTEMS
10. System Boot
Set default boot target to multi-user.target.

11. Bootloader
Configure GRUB so that the boot menu is displayed for 5 seconds.

12. Process & Logs
Save the top 7 CPU-consuming processes to /root/cputop7.txt.

Ensure persistent system journals.

13. Services
Start and enable rsyslog.

Stop and mask bluetooth service.

🟧 SECTION 5 — STORAGE
A new disk /dev/sdd (12 GB) is available.

14. Partitioning
Create a GPT partition /dev/sdd1 with size 4 GB.

15. LVM Setup
Create:

PV: /dev/sdd1

VG: devvg

LV: data3 size 3.5G

Create an XFS filesystem on data3.

16. Persistent Mount
Mount it permanently at /data/project using its UUID.

17. LV Extension
Extend LV data3 by 1 GB and grow filesystem.

18. Additional Storage
Create a 600M swap LV named lv_swap inside devvg.

Enable it permanently.

🟧 SECTION 6 — FILESYSTEMS & NETWORK FILESYSTEMS
19. Local Filesystems
Create an ext4 filesystem on /dev/sdd2 (size 2GB).

Mount at /legacy.

20. NFS
On remote.example.com, an export is available:

remote.example.com:/exports/teamshare

Mount temporarily at /mnt/teamtemp.

21. autofs
Configure autofs:

Master file: /etc/auto.master.d/team.autofs

Mount point: /teams

Map: /etc/auto.teams

Key: share

Target: remote.example.com:/exports/teamshare

🟧 SECTION 7 — SCHEDULING
22. Cron
Create a cron job for root:

Runs at 1:30 AM every day

Executes /usr/local/bin/dirsize.sh

Appends output to /var/log/homesizes.log

23. systemd Timer
Create a timer/unit pair rotatejournal:

Timer runs every hour

Unit must run /usr/local/bin/rotatejournal.sh

Script should archive /var/log/journal into /var/log/journal_archive_<timestamp>.tar.gz

🟧 SECTION 8 — NETWORKING
24. Network Configuration
Configure:

IP: 172.22.10.50/24

Gateway: 172.22.10.1

DNS: 8.8.8.8

25. Hostname
Set hostname to primary.example.com.

26. firewalld
Permanently allow:

ssh

nfs

http

Permanently block:

smtp service

🟧 SECTION 9 — USERS & GROUPS
27. User Setup
Create:

User engineer with password Engi123!

User tester with password Test123!

Create group devteam.
Add both users to this group.

28. Password Policies
Configure tester:

Max password age: 60 days

Min password age: 2 days

Warn: 10 days

29. Sudo Rules
Users in group devteam can run /usr/sbin/useradd without password.

🟧 SECTION 10 — SECURITY & SELinux
30. SSH Key Authentication
As root on primary, generate SSH keys.

Configure passwordless SSH from root@primary → root@remote.

31. SELinux Context Management
Fix SELinux contexts on /data/project to default.

32. SELinux Boolean
Enable SELinux boolean nis_enabled permanently.

33. SELinux Port
Allow the httpd service to run on TCP port 9090.

🟥 FINAL REQUIREMENT
47. Reboot the system.
Confirm all configurations survive reboot.

